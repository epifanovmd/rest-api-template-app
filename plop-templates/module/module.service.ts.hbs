import { NotFoundException } from "@force-dev/utils";

import { Injectable , sequelize } from "../../core";

import { {{properCase name}}, I{{properCase name}}Dto } from "./{{dashCase name}}.model";

@Injectable()
export class {{properCase name}}Service {
  async get{{properCase name}}ById(id: string): Promise<I{{properCase name}}Dto> {
    const {{camelCase name}} = await {{properCase name}}.findByPk(id);

    if (!{{camelCase name}}) {
      throw new NotFoundException();
    }

    return {{camelCase name}};
  }
}
