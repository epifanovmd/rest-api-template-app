export enum ECallType {
  VOICE = "voice",
  VIDEO = "video",
}

export enum ECallStatus {
  RINGING = "ringing",
  ACTIVE = "active",
  ENDED = "ended",
  MISSED = "missed",
  DECLINED = "declined",
}
