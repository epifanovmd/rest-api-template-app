export enum EChatType {
  DIRECT = "direct",
  GROUP = "group",
  CHANNEL = "channel",
}

export enum EChatMemberRole {
  OWNER = "owner",
  ADMIN = "admin",
  MEMBER = "member",
  SUBSCRIBER = "subscriber",
}
