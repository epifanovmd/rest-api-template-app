export enum ESyncEntityType {
  MESSAGE = "message",
  CHAT = "chat",
  CHAT_MEMBER = "chat_member",
  CONTACT = "contact",
  PROFILE = "profile",
}

export enum ESyncAction {
  CREATE = "create",
  UPDATE = "update",
  DELETE = "delete",
}
